services:
  officebooking:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      # See README.md for seeding options (SEED_ADMIN, SEED_DEMO_DATA)
    volumes:
      - officebooking-data:/app/data

volumes:
  officebooking-data:
